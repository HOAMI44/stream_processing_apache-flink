apiVersion: 1

datasources:
  - name: Weather Postgres
    uid: weather-postgres
    type: postgres
    access: proxy
    url: host.docker.internal:5432
    database: weather
    user: weather
    secureJsonData:
      password: weather
    jsonData:
      sslmode: disable
      postgresVersion: 1700
